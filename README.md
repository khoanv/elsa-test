# Server
- Start Docker to install Redis
    docker-compose up -d
- Install lib
    npm install
- Run server
    node src/server.js

# Frontend
- Install lib
    npm install
- Run web
    npm run start
- Web will run in http://localhost:3000